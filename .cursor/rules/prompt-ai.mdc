---
description: After completing a task, update the prompt.ai folder.
globs:
alwaysApply: false
---

## Documentation
When making code changes, update related documentation as needed.

## Prompt.AI
Mode switch:
- If `prompt.ai/development_log.template.md` exists, treat repo as template mode.
- If it does not exist, treat repo as project mode.

After each meaningful task:
- Template mode:
  - Do not append project/session history to `prompt.ai/development_log.md`
  - Prefer writing guidance and findings in `prompt.ai/research/`
  - If temporary local notes are needed, use `prompt.ai/session_notes.local.md` (gitignored)
- Project mode:
  - Add an entry to `prompt.ai/development_log.md`
  - Include what changed, why, and any follow-up items
- For releases, create `prompt.ai/vX.Y.Z_release_summary.md`
